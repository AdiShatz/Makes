import React, {useState} from "react";

import "./CreateBookButton.css";

const CreateBookButton = (props) => {        

    return (
      <button className='creat-book-button' >צור ספר</button>
    );
}
export default CreateBookButton;